---
title: 元数据
description: 与 Next.js 元数据 API 的使用方法。
---

> 本指南仅适用于 Next.js。

## 介绍

Next.js 提供了一套实用的工具集，可与 Fumadocs 实现灵活的使用体验。
Fumadocs 使用 Next.js Metadata API 进行 SEO 优化。

请务必阅读 Next.js 的 [Metadata 章节](https://nextjs.org/docs/app/building-your-application/optimizing/metadata) 以了解 Metadata API 的基础知识。

## Open Graph 图片

对于文档页面，Fumadocs 内置了元数据图片生成器。

您需要一个路由处理器来开始使用。

<include cwd meta='title="app/docs-og/[...slug]/route.tsx"'>
  ../../examples/next-mdx/app/docs-og/[...slug]/route.tsx
</include>

> 我们需要在 slugs 末尾添加 `image.png`，以便通过 `/docs-og/my-page/image.png` 访问它。

在您的文档页面中，将图片添加到元数据中。

```tsx title="app/docs/[[...slug]]/page.tsx"
import { notFound } from 'next/navigation';
import { source } from '@/lib/source';

export async function generateMetadata({
  params,
}: {
  params: Promise<{ slug?: string[] }>;
}) {
  const { slug = [] } = await params;
  const page = source.getPage(slug);
  if (!page) notFound();

  const image = ['/docs-og', ...slug, 'image.png'].join('/');
  return {
    title: page.data.title,
    description: page.data.description,
    openGraph: {
      images: image,
    },
    twitter: {
      card: 'summary_large_image',
      images: image,
    },
  };
}
```

### 字体

您还可以自定义字体，内置生成器也支持 Satori 的选项。

```ts
import { generateOGImage } from 'fumadocs-ui/og';

generateOGImage({
  fonts: [
    {
      name: 'Roboto',
      // Use `fs` (Node.js only) or `fetch` to read the font as Buffer/ArrayBuffer and provide `data` here.
      data: robotoArrayBuffer,
      weight: 400,
      style: 'normal',
    },
  ],
});
```
