---
title: Zoomable Image
description: 在您的文档中启用图片缩放功能
preview: zoomImage
---

<Installation name="image-zoom" />

## 使用方法

在您的 MDX 组件中将 `img` 替换为 `ImageZoom`。

```tsx title="mdx-components.tsx"
import { ImageZoom } from 'fumadocs-ui/components/image-zoom';
import defaultComponents from 'fumadocs-ui/mdx';
import type { MDXComponents } from 'mdx/types';

export function getMDXComponents(components?: MDXComponents): MDXComponents {
  return {
    ...defaultComponents,
    img: (props) => <ImageZoom {...(props as any)} />,
    ...components,
  };
}
```

现在所有图片将自动启用缩放功能。

```mdx
![Test](/banner.png)
```

### 图片优化

在 Next.js 中，如果未指定 [`sizes` 属性](https://nextjs.org/docs/app/api-reference/components/image#sizes)，将为 `<Image />` 组件定义默认值。
