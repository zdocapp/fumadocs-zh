---
title: Orama (默认)
description: 由 Orama 驱动的默认搜索引擎。
---

## 概述

Fumadocs 开箱即用地配置了 [Orama 搜索引擎](/docs/headless/search/orama)。

它通过 API 端点（在服务器上运行）工作，或者为静态网站使用静态缓存的 JSON 文件。

## 设置

创建搜索对话框。

<Tabs items={['fetch (默认)', 'static']}>

    <Tab>

UI 已默认配置，您也可以重新创建以进行进一步自定义：

<include meta='title="components/search.tsx"'>fetch.tsx</include>

    </Tab>

    <Tab id='static'>

对于静态导出，您可以在搜索服务器上配置[静态模式](/docs/headless/search/orama#static-export)，并使用 `static` 客户端：

```package-install
@orama/orama
```

<include meta='title="components/search.tsx"'>./static.tsx</include>

    </Tab>

</Tabs>

<include>.shared.mdx</include>

### 标签筛选

可选地，您可以添加按标签筛选结果的 UI。在搜索服务器上配置[标签筛选器](/docs/headless/search/orama#tag-filter)并添加以下内容：

<include>.tag-filter.mdx</include>
