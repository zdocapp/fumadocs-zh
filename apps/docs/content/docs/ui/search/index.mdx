---
title: 搜索
description: 在您的文档中实现文档搜索功能
---

## 搜索界面

您可以从根提供程序自定义一些配置。

例如，禁用搜索界面：

```tsx title="app/layout.tsx"
import { RootProvider } from 'fumadocs-ui/provider';
import type { ReactNode } from 'react';

export default function Layout({ children }: { children: ReactNode }) {
  return (
    <html>
      <body>
        <RootProvider
          search={{
            enabled: false, // [!code ++]
          }}
        >
          {children}
        </RootProvider>
      </body>
    </html>
  );
}
```

如需进一步自定义，请参阅[搜索客户端](#search-client)。

### 快捷键

自定义触发搜索对话框的快捷键，默认为 <kbd>⌘</kbd> <kbd>K</kbd> 或 <kbd>Ctrl</kbd> <kbd>K</kbd>。

```tsx
import { RootProvider } from 'fumadocs-ui/provider';

<RootProvider
  search={{
    hotKey: [
      {
        display: 'K',
        key: 'k', // key code, or a function determining whether the key is pressed
      },
    ],
  }}
>
  {children}
</RootProvider>;
```

## 搜索客户端

您可以根据搜索引擎选择和配置搜索客户端，默认为 Orama 搜索。

<DocsCategory />

### 社区集成

由社区维护的集成列表。

- [Trieve Search](/docs/headless/search/trieve)

### 高亮匹配项

搜索集成可以提供 `contentWithHighlights` 来高亮显示匹配项。

根据上述指南配置搜索客户端界面后，您可以自定义其渲染方式。

```tsx title="components/search.tsx"
<SearchDialogList
  items={query.data !== 'empty' ? query.data : null}
  // [!code highlight:8]
  Item={(props) => (
    <SearchDialogListItem
      {...props}
      renderHighlights={(highlights) => {
        // ...
      }}
    />
  )}
/>
```
