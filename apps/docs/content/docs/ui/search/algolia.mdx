---
title: Algolia
description: 在 Fumadocs UI 中使用 Algolia。
---

## 概述

有关设置指南，请参阅 [集成 Algolia 搜索](/docs/headless/search/algolia)。

虽然我们通常建议使用他们的客户端 SDK 构建自己的搜索功能，但您也可以插入内置的对话框界面。

## 设置

创建一个搜索对话框，将 `appId`、`apiKey` 和 `indexName` 替换为您所需的值。

<include meta='title="components/search.tsx"'>./algolia.tsx</include>

<Callout title="Note" className='mt-4'>

    `useDocsSearch()` doesn't use instant search (their official
    Javascript client).

</Callout>

<include>.shared.mdx</include>

### 标签筛选

可选地，您可以添加按标签筛选结果的 UI。在搜索服务器上配置 [标签筛选器](/docs/headless/search/algolia#tag-filter) 并添加以下内容：

<include>.tag-filter.mdx</include>
